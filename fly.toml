app = 'carl-fung'
primary_region = 'sjc'

[build]

[env]
  SSH_PORT = '2222'
  SSH_HOST = '0.0.0.0'
  SSH_HOST_KEY = '/data/ssh/id_ed25519'

[mounts]
  source = 'ssh_host_keys'
  destination = '/data/ssh'

[[services]]
  protocol = 'tcp'
  internal_port = 2222

  [[services.ports]]
    port = 22
